{
  "name": "Vectorac",
  "version": "1.0.0",
  "author": "reco_luan",
  "scripts": {
    "build:slime-worker": "node scripts/build-slime-worker.js",
    "predev": "npm run build:slime-worker",
    "dev": "NODE_OPTIONS=--openssl-legacy-provider vuepress dev . --open --host \"localhost\"",
    "prebuild": "npm run build:slime-worker",
    "build": "NODE_OPTIONS=--openssl-legacy-provider vuepress build .",
    "postbuild": "node scripts/check-slime-worker.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "vuepress": "1.5.4",
    "vuepress-theme-reco": "1.5.7"
  },
  "dependencies": {
    "three": "0.134.0",
    "vue-class-component": "^7.2.3"
  },
  "description": "Web"
}
